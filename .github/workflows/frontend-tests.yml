name: Frontend tests

on:
  pull_request:
  push:
    branches:
      - dev
      - main

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/my-rag-app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/my-rag-app/package-lock.json
      - run: npm ci
      - run: npm run test
      - run: npm run build

  e2e:
    needs: quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/my-rag-app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/my-rag-app/package-lock.json
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/my-rag-app/playwright-report/
          if-no-files-found: ignore
          retention-days: 14
      - name: Upload Playwright traces and screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: frontend/my-rag-app/test-results/
          if-no-files-found: ignore
          retention-days: 14
