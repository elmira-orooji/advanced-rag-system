import { useEffect, useState } from "react";
import { Bot, Check, FileStack, Pencil, Plus, Power, Sparkles, Trash2, X } from "lucide-react";
import { motion } from "framer-motion";
import { useTranslation } from "react-i18next";
import toast from "react-hot-toast";
import ChatInput from "../components/ChatInput";
import ChatWindow from "../components/ChatWindow";
import { authService } from "../services/authService";
import { assistantService, type AssistantPayload, type CustomAssistant } from "../services/assistantService";
import type { DocumentSet } from "../services/knowledgeService";
import type { ChatMessage } from "../types/chat";

export default function AssistantsPage() {
  const { i18n } = useTranslation(); const fa = i18n.language.startsWith("fa"); const admin = authService.getUser()?.role === "admin";
  const [items, setItems] = useState<CustomAssistant[]>([]); const [sets, setSets] = useState<DocumentSet[]>([]); const [selected, setSelected] = useState<CustomAssistant | null>(null); const [editing, setEditing] = useState<CustomAssistant | "new" | null>(null); const [messages, setMessages] = useState<ChatMessage[]>([]); const [thinking, setThinking] = useState(false); const [loading, setLoading] = useState(true); const [mobileChat, setMobileChat] = useState(false);
  const c = fa ? { eyebrow: "دستیارهای تخصصی", title: "دستیارها", subtitle: "دستیارهای هوشمند با دستورالعمل و منابع دانشی اختصاصی.", add: "دستیار جدید", empty: "هنوز دستیاری ساخته نشده است", choose: "یک دستیار را برای شروع گفتگو انتخاب کنید", knowledge: "مجموعه دانش", inactive: "غیرفعال", active: "فعال", edit: "ویرایش", remove: "حذف", noKnowledge: "بدون مجموعه دانش" } : { eyebrow: "Specialized AI", title: "Assistants", subtitle: "Purpose-built assistants with focused instructions and trusted knowledge.", add: "New assistant", empty: "No assistants have been created yet", choose: "Choose an assistant to start a focused conversation", knowledge: "knowledge sets", inactive: "Inactive", active: "Active", edit: "Edit", remove: "Delete", noKnowledge: "No knowledge assigned" };
  const load = async () => { try { const [assistants, knowledgeSets] = await Promise.all([assistantService.list(), assistantService.listSets()]); setItems(assistants); setSets(knowledgeSets); setSelected((current) => assistants.find((x) => x.id === current?.id) || assistants.find((x) => x.is_active) || null); } catch (e) { toast.error((e as Error).message); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const choose = (item: CustomAssistant) => { if (!item.is_active) return; setSelected(item); setMessages([]); setMobileChat(true); };
  const ask = async (content: string) => { if (!selected) return; setMessages((m) => [...m, { id: crypto.randomUUID(), role: "user", content, createdAt: new Date().toISOString() }]); setThinking(true); try { const result = await assistantService.ask(selected.id, content); setMessages((m) => [...m, { id: crypto.randomUUID(), role: "assistant", content: result.answer, responseId: result.response_id, grounded: result.grounded, createdAt: new Date().toISOString(), sources: result.citations.map((x) => ({ id: x.chunk_id, citationId: x.id, documentId: x.document_id, title: x.filename, chunkIndex: x.chunk_index, excerpt: x.excerpt, score: x.score, page: x.page, section: x.section })) }]); } catch (e) { toast.error((e as Error).message); } finally { setThinking(false); } };
  const remove = async (item: CustomAssistant) => { if (!confirm(fa ? `دستیار «${item.name}» حذف شود؟` : `Delete “${item.name}”?`)) return; try { await assistantService.remove(item.id); toast.success(fa ? "دستیار حذف شد" : "Assistant deleted"); await load(); } catch (e) { toast.error((e as Error).message); } };
  return <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="flex h-full min-h-0 overflow-hidden">
    <section className="flex min-w-0 flex-1 flex-col px-4 py-5 sm:px-6 lg:px-8"><header className="flex shrink-0 items-end justify-between"><div><div className="mb-2 flex items-center gap-2 text-[11px] font-bold uppercase tracking-[.2em] text-[#a995eb]"><span className="size-1.5 rounded-full bg-[#8f78d8]" />{c.eyebrow}</div><h1 className="text-3xl font-semibold tracking-[-.045em] sm:text-4xl">{c.title}</h1><p className="mt-2 text-sm text-white/35">{c.subtitle}</p></div>{admin && <button onClick={() => setEditing("new")} className="flex h-10 items-center gap-2 rounded-xl bg-[#32127A] px-4 text-xs font-semibold shadow-[0_10px_28px_rgba(50,18,122,.3)]"><Plus size={15} />{c.add}</button>}</header>
      <div className="mt-6 grid min-h-0 flex-1 auto-rows-min grid-cols-1 gap-3 overflow-y-auto pb-2 sm:grid-cols-2 xl:grid-cols-3">{loading ? <div className="col-span-full grid min-h-48 place-items-center"><span className="size-5 animate-spin rounded-full border-2 border-white/10 border-t-[#a995eb]" /></div> : items.length ? items.map((item) => <article key={item.id} onClick={() => choose(item)} className={`group relative cursor-pointer rounded-[22px] border p-5 transition ${selected?.id === item.id ? "border-[#8f78d8]/45 bg-[#32127A]/20 shadow-[0_18px_45px_rgba(50,18,122,.18)]" : "border-white/[.08] bg-white/[.03] hover:bg-white/[.05]"} ${!item.is_active ? "opacity-55" : ""}`}><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-2xl border border-[#8f78d8]/20 bg-[#32127A]/25 text-[#b6a7ef]"><Bot size={20} /></span><div className="flex items-center gap-1">{!item.is_active && <span className="rounded-full border border-white/10 px-2 py-1 text-[9px] text-white/35">{c.inactive}</span>}{admin && <button onClick={(e) => { e.stopPropagation(); setEditing(item); }} className="app-icon-button grid size-8 place-items-center rounded-lg text-white/25 hover:text-white"><Pencil size={13} /></button>}{admin && <button onClick={(e) => { e.stopPropagation(); void remove(item); }} className="app-icon-button grid size-8 place-items-center rounded-lg text-white/25 hover:text-rose-300"><Trash2 size={13} /></button>}</div></div><h2 className="mt-4 text-base font-semibold text-white/85">{item.name}</h2><p className="mt-2 line-clamp-2 min-h-10 text-xs leading-5 text-white/30">{item.description || item.instructions}</p><div className="mt-4 flex items-center gap-2 border-t border-white/[.06] pt-3 text-[10px] text-white/30"><FileStack size={12} /><span className="truncate">{item.document_set_names.length ? `${item.document_set_names.length} ${c.knowledge}` : c.noKnowledge}</span></div></article>) : <div className="col-span-full grid min-h-60 place-items-center rounded-[24px] border border-dashed border-white/10 text-sm text-white/30"><span className="flex flex-col items-center gap-3"><Bot size={25} />{c.empty}</span></div>}</div>
    </section>
    {mobileChat && <button onClick={() => setMobileChat(false)} className="fixed inset-0 z-40 bg-black/65 backdrop-blur-sm xl:hidden" />}
    <aside className={`fixed bottom-0 end-0 top-16 z-50 flex w-[min(100%,390px)] shrink-0 flex-col border-s border-white/[.08] bg-[rgba(8,6,12,.96)] transition-transform xl:relative xl:inset-auto xl:z-auto xl:flex xl:bg-black/15 ${mobileChat ? "translate-x-0" : "translate-x-full xl:translate-x-0"}`}>{selected ? <><header className="flex h-20 shrink-0 items-center gap-3 border-b border-white/[.07] px-5"><span className="grid size-10 place-items-center rounded-xl bg-[#32127A]/30 text-[#b6a7ef]"><Sparkles size={18} /></span><div className="min-w-0 flex-1"><h2 className="truncate text-sm font-semibold">{selected.name}</h2><p className="mt-1 truncate text-[10px] text-white/30">{selected.document_set_names.join(" · ") || c.noKnowledge}</p></div><button onClick={() => setMobileChat(false)} className="app-icon-button grid size-9 place-items-center rounded-xl text-white/40 xl:hidden"><X size={16} /></button></header><div className="min-h-0 flex-1 p-4">{messages.length ? <ChatWindow messages={messages} isThinking={thinking} /> : <div className="flex h-full flex-col items-center justify-center text-center"><span className="grid size-12 place-items-center rounded-2xl border border-[#8f78d8]/20 bg-[#32127A]/20 text-[#b6a7ef]"><Bot size={21} /></span><p className="mt-4 max-w-64 text-xs leading-5 text-white/35">{selected.description || c.choose}</p></div>}</div><div className="shrink-0 border-t border-white/[.07] p-4"><ChatInput disabled={thinking} onSend={ask} /></div></> : <div className="grid h-full place-items-center px-8 text-center text-xs leading-5 text-white/30">{c.choose}</div>}</aside>
    {editing && <AssistantDialog item={editing === "new" ? undefined : editing} sets={sets} fa={fa} onClose={() => setEditing(null)} onSaved={async () => { setEditing(null); await load(); }} />}
  </motion.div>;
}

function AssistantDialog({ item, sets, fa, onClose, onSaved }: { item?: CustomAssistant; sets: DocumentSet[]; fa: boolean; onClose: () => void; onSaved: () => void }) {
  const [name, setName] = useState(item?.name || ""); const [description, setDescription] = useState(item?.description || ""); const [instructions, setInstructions] = useState(item?.instructions || ""); const [selectedSets, setSelectedSets] = useState<string[]>(item?.document_set_ids || []); const [active, setActive] = useState(item?.is_active ?? true); const [saving, setSaving] = useState(false);
  const valid = name.trim().length >= 2 && instructions.trim().length >= 10;
  const submit = async (e: React.FormEvent) => { e.preventDefault(); if (!valid) return; setSaving(true); const payload: AssistantPayload = { name: name.trim(), description: description.trim(), instructions: instructions.trim(), document_set_ids: selectedSets, is_active: active }; try { item ? await assistantService.update(item.id, payload) : await assistantService.create(payload); toast.success(fa ? "دستیار ذخیره شد" : "Assistant saved"); onSaved(); } catch (error) { toast.error((error as Error).message); } finally { setSaving(false); } };
  const inputClass = "h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3 text-sm text-white outline-none transition placeholder:text-white/20 focus:border-[#8f78d8]/50";
  return <div className="fixed inset-0 z-[80] grid place-items-center bg-black/75 p-4 backdrop-blur-md" onMouseDown={onClose}><motion.form initial={{ opacity: 0, scale: .97 }} animate={{ opacity: 1, scale: 1 }} onSubmit={submit} onMouseDown={(e) => e.stopPropagation()} className="app-glass-panel max-h-[90dvh] w-full max-w-xl overflow-y-auto rounded-[26px] border border-white/10 p-6"><div className="flex justify-between"><div><span className="grid size-10 place-items-center rounded-xl bg-[#32127A]/30 text-[#b6a7ef]"><Bot size={18} /></span><h2 className="mt-4 text-xl font-semibold">{fa ? (item ? "ویرایش دستیار" : "ساخت دستیار تخصصی") : (item ? "Edit assistant" : "Create specialized assistant")}</h2></div><button type="button" onClick={onClose} className="app-icon-button grid size-9 place-items-center rounded-xl text-white/40"><X size={16} /></button></div><div className="mt-6 grid gap-4"><Field label={fa ? "نام" : "Name"}><input value={name} onChange={(e) => setName(e.target.value)} className={inputClass} /></Field><Field label={fa ? "توضیح کوتاه" : "Short description"}><input value={description} onChange={(e) => setDescription(e.target.value)} className={inputClass} /></Field><Field label={fa ? "دستورالعمل تخصصی" : "Specialized instructions"}><textarea rows={5} value={instructions} onChange={(e) => setInstructions(e.target.value)} placeholder={fa ? "نقش، محدوده پاسخ و لحن دستیار را مشخص کنید..." : "Define the role, answer boundaries, and tone..."} className={`${inputClass} h-auto resize-none py-3`} /></Field><Field label={fa ? "مجموعه‌های دانش" : "Knowledge sets"}><div className="grid gap-2 sm:grid-cols-2">{sets.map((set) => { const checked = selectedSets.includes(set.id); return <button type="button" key={set.id} onClick={() => setSelectedSets(checked ? selectedSets.filter((id) => id !== set.id) : [...selectedSets, set.id])} className={`flex items-center gap-2 rounded-xl border p-3 text-start text-xs ${checked ? "border-[#8f78d8]/35 bg-[#32127A]/25 text-white/75" : "border-white/[.08] text-white/35"}`}><span className={`grid size-6 place-items-center rounded-lg ${checked ? "bg-[#32127A]" : "bg-white/[.04]"}`}>{checked ? <Check size={12} /> : <FileStack size={12} />}</span><span className="truncate">{set.name}</span></button>})}</div></Field><button type="button" onClick={() => setActive(!active)} className="flex items-center justify-between rounded-xl border border-white/[.08] p-3 text-xs text-white/55"><span className="flex items-center gap-2"><Power size={14} />{fa ? "دستیار فعال باشد" : "Assistant is active"}</span><span className={`relative h-5 w-9 rounded-full transition ${active ? "bg-[#32127A]" : "bg-white/10"}`}><span className={`absolute top-0.5 size-4 rounded-full bg-white transition ${active ? "end-0.5" : "start-0.5"}`} /></span></button></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-xl px-4 py-2.5 text-xs text-white/40">{fa ? "انصراف" : "Cancel"}</button><button disabled={!valid || saving} className="rounded-xl bg-[#32127A] px-5 py-2.5 text-xs font-semibold disabled:opacity-40">{saving ? "…" : fa ? "ذخیره دستیار" : "Save assistant"}</button></div></motion.form></div>;
}
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs font-semibold text-white/50">{label}<div className="mt-2">{children}</div></label>; }
