[pytest]
testpaths = tests
markers =
    unit: fast, isolated tests for domain and service behavior
    integration: API and infrastructure-bound tests that do not require live provider credentials
    cloud: opt-in tests that call a live cloud provider and may consume quota
